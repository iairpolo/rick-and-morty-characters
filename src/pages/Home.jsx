import React from 'react';

const Home = () => (
  <section className='Main'>
    <form action='submit'>
      <input placeholder='¿Qué personaje quieres buscar?' />
      <button>Buscar</button>
    </form>
  </section>
);

export default Home;
